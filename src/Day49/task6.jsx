// Store
// Holds the current state of the counter.
// Example: { count: 0 }

// Action
// An object describing what happened.
// Example: { type: "INCREMENT", payload: 1 }

// Reducer
// A pure function that takes current state + action → returns new state.
// Updates the counter based on the action type.

// Dispatch
// Sends the action to the store, triggering the reducer.

// useSelector (React-Redux)
// Reads the current state from the store so UI can display the updated counter.